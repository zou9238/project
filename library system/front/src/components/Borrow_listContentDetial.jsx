export const Borrow_listContentDetial = (props) => {

    return (
        <div  {...props.borrow_list.id}>
            <p><strong>Borrow_listContentDetial</strong> </p>
            {props.borrow_list.name}
        </div>
    );
}
