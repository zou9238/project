export const SearchContentDetial = (props) => {

    return (
        <div  {...props.search.id}>
            <p><strong>SearchContentDetial</strong> </p>
            {props.search.name}
        </div>
    );
}
